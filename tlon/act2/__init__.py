"""ACT 2 — **`D_ctx`: in-context convention between native-Tlön models.**

⛔ Research, not product. Nothing here may write to `tlon/product/`, and no
Act-2 lexicon may reach the frozen product lexicon (PREREG §6, axis 4).

⭐⭐ RULED 2026-08-24: ACT 2 IS `D_ctx`, NOT `D_w`, AND THE WORD "DRIFT" WAS
QUIETLY PROMISING THE HARDER ONE.

Fine-tune once, freeze, run the arena — **the weights do not change during a
conversation**, so whatever moves is the CONTEXT the models condition on, not
the models. That is in-context convention formation: two speakers who already
know the language co-adapting *in their shared history*. It is real, measurable,
and it is **the LLM-scale replication of H2** — the pact that formed around a
distinction the grammar could not express, banked at 22× MDE. Act 2 asks whether
that proven phenomenon replicates and becomes watchable when the speakers are
models.

⛔ `D_w` — the models *changing* as they talk, gradient steps between turns, the
language reshaping the network — is **PARKED AS A SEPARATE PROJECT, NAMED NOT
BURIED.** It needs online updates inside the arena loop, with its own stability
problems and its own falsifiers for "is this weight change real or is it
catastrophic drift". Reopening condition: an arena that can take gradient steps
between turns without the run becoming a study of training instability.

⭐ WHAT THE FINE-TUNE ACTUALLY BUYS, then, is not `D_w`. It is that the mapping
moves into the weights, which (a) strips the GATE's fingerprints off the
production half — no retry loop standing between the model and the measurement —
and (b) makes the comprehension half well-posed **for the first time**, because a
cardless model decodes from what it knows rather than from a table we handed it.
Both are prerequisites for any drift number meaning what it says.

⭐ AND THE DREAM SURVIVES INTACT. *"Two models who THINK and SPEAK in Tlön
talking to each other and drifting in public."* The THINKING and SPEAKING is the
fine-tune — native, cardless, real. The DRIFTING is the in-context convention.
Weight drift was never required by the dream; it was an ambiguity in the word.
"""
