# RESUME — 2026-09-10

⭐ **START HERE.** Supersedes `RESUME_2026_09_09.md` (still correct for the Qwen
single-card ladder and its machinery lessons).

---

## ⛔⛔ FIRST ACTION: FIX THE PAD/EOS COLLATOR BUG, THEN RE-RUN MISTRAL 3a

`act2_finetune.py:555-556`:

```python
if tok.pad_token is None:
    tok.pad_token = tok.eos_token
```

…and `DataCollatorForLanguageModeling` masks labels **by token id**. When pad
*is* eos it masks every genuine end-of-sequence, so **the model is never trained
to stop**. Verified on CPU:

```
Olmo-3-7B        label at the REAL eos = 100257   trained to stop normally
Mistral-7B-v0.3  label at the REAL eos =   -100   NEVER TRAINED TO STOP
```

⛔ That destroyed run 3a (~$6): `speak 0%`, `render 1.6%`, `choose 256/256
unanswered`, one distinct output for twelve inputs. **The weights were FINE** —
`fraction_changed` 0.99995, `delta_norm` 18.697, rms 3.166e-04 vs Qwen's
3.118e-04. Training worked; stopping was destroyed.

⭐⭐ **THE LINE WAS WRITTEN WHEN QWEN WAS THE ONLY BASE.** Qwen has a distinct
pad token, so it never fired in the whole arc. **A held constant became a
variable and nothing said so** — the campaign's own thesis, reproduced inside
the harness that was running the campaign.

**FIX:** mask labels by **position** (`attention_mask == 0`), never by token id.
⭐ Correct for every base, and Qwen's and OLMo's runs stay comparable because
neither was ever affected (both have distinct pad tokens — checked, not assumed).

⛔ **3a is NOT a Mistral verdict and must never be recorded as one.**

---

## Where the campaign is

**PREREG `91956dbe`**, re-locked **`5f4a554c`** (AMENDMENT A: the Mistral base
swapped from the multimodal Ministral-3-8B to text-only Mistral-7B-v0.3).
Results: `docs/RESULTS_CAMPAIGN_FAMILY_2A_3A_2026_09_10.md`.

| cell | state |
|---|---|
| Qwen layers | release does not install — **unchanged** |
| Qwen mapping | untouchable at 1e-5 and 5e-6 — **unchanged** |
| **OLMo layers (2a)** | ⛔ **NOT ESTABLISHED** — F-LOCAL fired, render 51.6% |
| OLMo mapping (2b) | not fired; cell EMPTY, never a collapse |
| **Mistral layers (3a)** | ⛔ **INSTRUMENT FAULT** — re-run pending |
| Mistral mapping (3b) | gated on 3a's F-LOCAL |

⛔⛔ **ZERO cross-family release datapoints.** The "Qwen fact or LLM fact"
question now rests entirely on Mistral's re-run.

### ⭐ Run 2a is a real result and the gate is why

OLMo trained perfectly — dose **100.0%** matched to Qwen's rung 1a, 12/12 chains,
lag z +23.38 / +8.89 / +1.71 / +0.99, n_pairs 108/96/84/72, every cell
resolvable. **And its +8.89 is meaningless**, because F-LOCAL fired:

```
speak   100.0%  free generation
render   51.6%  directed production, threshold 0.90
```

⭐ **OLMo speaks Tlön fluently and cannot translate on demand.** The class system
holds when it chooses what to say and breaks when the meaning is imposed. Release
requires faithful production *in response to a provocation* — directed
production, the exact failure — so the number cannot separate release-failure
from render-failure.

⛔ Without the gate, 2a enters the matrix as *cross-family confirmation, worse
than Qwen*. A false datapoint from a non-speaker, on the gate's first real
application. **The gate's placement at the LAYER rung (the forgiving config Qwen
clears) is what makes OLMo's failure a BASE verdict rather than a config
artifact.**

### ⭐ Separate finding, filed on its own terms

**Faithful directed Tlön production is substrate-dependent across capable 7B
families at matched config and dose.** Qwen clears both; OLMo clears free
generation at 100% and fails directed rendering. ⚠️ It *rhymes* with the mapping
finding — both point at meaning↔form as the fragile locus — recorded as a rhyme,
not a claim.

---

## ⭐⭐ Machinery built on 09-09/10 (1782 passing)

1. **`UnscoreableLag`** — an empty lag cell is a named diagnosis, not a
   `ZeroDivisionError`. ⛔ The crash was the cheap half; the `nan` "fix" would
   have rendered an unmeasured lag as *"release persists"* or *"perceive
   collapsed"*.
2. **Scoreability DERIVED, not picked** — a cell is readable only if
   `resolving_power` (the z of the maximum attainable profile) reaches the
   LOCKED threshold. ⛔ `sd ~ sigma/sqrt(n)` means a small cell CANNOT pass lag-1
   and CANNOT fail lag≥2 — the bias runs in a fixed direction and spells row (d)
   by arithmetic. Containment checked: no fired verdict moves.
3. **The sampling stream is seeded** — `--seed` named a seed that did not control
   generation; every profile in the arc before `6988d06` is one undocumented draw.
4. **`pipeline_fullft_read.sh`** — re-read a persisted object without retraining
   or overwriting it.
5. **Multi-stack scope** — a multimodal base pools two layer stacks into an index
   set that looks CONTIGUOUS; `unfreeze_top=14` trained 36 vision tensors.
   `stack=` restricts the SELECTION, not only the count.
6. **`lint_step_scope.py`** — every `step` declares `# SCOPE: any|layers|mapping`
   and mode-specific steps must sit inside a matching guard. Retires a
   five-instance class.

---

## Storage — architecture fixed

⭐ **80.67 GB archived to `R:\tlon_archive`, sha256-verified 4/4, then deleted
from the hub.** Live tree 85.58 → 4.91 GB; billed 85.52 → **25.93 GB**.
Readings retained (weight_delta, factorial, manifests — the findings live there).
⛔ The hub is only in the loop because a Lambda box takes its disk on
self-terminate; the local machine was never the constraint (D: 435 GB, R: 1.2 TB).

---

## ⏭ OPEN

1. ⛔⛔ **FIX THE COLLATOR** (mask by position), then **re-run Mistral 3a**.
2. **A preflight that LOADS the model** — every preflight passed before 3a died
   because none of them load it; VRAM plans from param counts, `cell_guard` and
   `hub_capacity` query the hub, `corpus` is CPU. The first thing touching the
   architecture is the training leg. ⭐ Would have caught Ministral-3's
   `AutoModelForCausalLM` refusal at minute zero (~$0.45).
3. **OLMo-recovery held contingent on Mistral** — its pre-registered set is
   (1) more contrastive negatives (2) curriculum fine-tune (3) bigger backbone.
   Needs its own prereg. Do NOT spend until Mistral's F-LOCAL is known.
4. **Operationalise "materially"** — method settled by item 2 of the machinery
   list (minimum resolvable difference from the run's own null, derived).
5. **The chains are not persisted** — only the aggregate null survives, so a
   fired run's cells cannot be re-analysed exactly.
6. **§7.1 all-28 via FSDP** — untested, negative-EV on capacity grounds.
7. **Three decisions from `RESUME_2026_09_04.md`** remain untouched.

---

## Cost

**~$21 on 09-10**: OLMo 2a ~$6.30 (a real result), Mistral 3a ~$6 (lost to the
collator bug), two aborted starts ~$2.45, Run 0 re-read ~$0.85, setup. ~$73
across the arc. ⭐ Every box self-terminated.
