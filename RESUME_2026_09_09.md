# RESUME — 2026-09-09

⭐ **START HERE.** Supersedes `RESUME_2026_09_08.md` (still correct for the
capacity result and its machinery lessons).

---

## ⛔ RUN 0 FIRED AND RETURNED NO VERDICT

**`e91f7c11`**, box `417b4a96…` (PCIe H100, us-west-3, pinned `82fa586`),
trained 01:47→02:41, **~$6**, self-terminated. **Nothing is billing.**

- ⭐ **Weights are SAFE — 17.42 GB verified on the hub** at
  `fwmap6-s20624/`, persisted *before* the crash. Persist-before-reads turned
  this into a lost READ, not a lost run.
- **F-LOCAL FIRED at 5e-6 as well as 1e-5.** Halving the LR did not save the
  speaker: comprehension 0.527 @1e-5 → 0.543 @5e-6, all items answered, both
  times.
- ⛔ **`lag_epoch1` CRASHED**, so there is no lag profile and no verdict:

      transient.py:324  permutation_null
      means.append(sum(len(x & y) for x, y in zip(a, b)) / len(a))
      ZeroDivisionError            # len(a) == 0 -- the chain set is EMPTY

  The speaker produced nothing the instrument could parse into chains.

⛔ **Do not read a direction into "lower LR, more degenerate."** One
observation, a crash rather than a measurement, no verdict. What is established
is only that the object is degenerate enough to leave the lag instrument with
nothing to score.

✅ **FIXED — see `docs/UNSCOREABLE_LAG_2026_09_09.md`.** *"The speaker emitted
nothing scoreable"* is now a named diagnosis (`UnscoreableLag`) carrying its
chain lengths, and it cannot be turned into a number by any consumer. ⛔ The
crash was the *cheap* half of the bug; the expensive half was that the obvious
fix — return `nan` — would have rendered an unmeasured lag as **"release
persists"** or **"perceive collapsed"**, a fabricated finding with a threshold
attached. Both halves are red-proofed.

⚠️ **The §5 third branch is live but UNSTATED**: *"the mapping cannot be touched
without collapse at either tested LR."* It needs a lag profile to be claimed.
⛔ §5 forbids a third dial-back without a new prereg saying what a third point
would establish.

**Delta for the record:** `delta_norm` 14.813, rms 4.4869e-04 (vs rung 2's
18.208 / 5.5152e-04 — 81% as far). `embed_tokens` moved 20/4096 at BOTH LRs,
the sparsity prediction reproducing exactly; only the magnitude changed
(dn 1.966 → 1.074).

---

## The five fired runs

| run | prereg | scope | LR | lag1 | lag2 | f_local | verdict |
|---|---|---|---|---|---|---|---|
| 1a | `a0450b36` | 14L | 1e-5 | 24.046 | **5.785** | PASS | floored · e2 incoherent |
| 1b | `9ccf98d6` | 19L | 5e-6 | 21.192 | 4.844 | PASS | floored, UNDER-DOSED |
| 1b′ | `bd435b37` | 19L | 1e-5 | 23.352 | **5.966** | PASS | floored |
| 2 | `c2a4f0ca` | mapping | 1e-5 | **2.232** | 0.681 | **FAIL** | **perceive killed** |
| **Run 0** | **`e91f7c11`** | mapping | **5e-6** | — | — | **FAIL** | ⛔ **NO VERDICT — lag crashed** |

**Layer capacity: answered, NO.** 19L at matched dose gave lag2 5.966 vs 14L's
5.785 — up, not down. ⛔ Do not buy FSDP-all-28 on capacity grounds; it stays
**untested**, not floored.

**The mapping: STILL UNANSWERED, at both tested LRs.** Rung 2 did not floor, it
**collapsed**. Run 0 (the 5e-6 dial-back) collapsed further and its read
crashed. ⛔ Two runs, no mapping verdict.

⛔⛔ **D6's architectural floor is untouched by all of it.**

---

## ⭐⭐ THE CAMPAIGN (agreed, partly locked)

**Is the release floor a Qwen fact or an LLM fact?** Base model has been an
unrecorded constant for the whole arc.

- **Run 0** — `e91f7c11`, Qwen mapping @5e-6. **The ANCHOR**, and it FIRED
  WITHOUT PRODUCING ONE (see top). ⛔ So the campaign's scale leg has no 7B
  mapping baseline to compare against, and Run 3 CANNOT be locked until that is
  resolved.
- **Run 2** — different family. ⛔ **BLOCKED ON A DECISION-TIME LICENCE CHECK.**
  Criteria, in order: **permissive licence (Apache-2.0/MIT), verified current —
  DISQUALIFYING, not a tiebreak** (public installation); ~7B-class so the family
  axis is not confounded with scale; furthest-from-Qwen among the clearing
  options. ⛔ **Backbone is Nate's call, every time** — and the scope doc says
  licence terms must be checked at decision time, never recalled.
- **Run 3** — scale. **Qwen 14B, MAPPING rung, one 80 GiB card**. ⛔ BLOCKED:
  it was to be locked against Run 0's verdict, which does not exist. ⭐
  Opportunistic upgrade to **14B LAYER rungs on a B200** (180 GB, $6.99/h) if
  capacity appears — it showed NONE all night.

⛔ **F-LOCAL GATE, CORRECTED — speakerhood is established at the LAYER rung.**
As first written ("fails F-LOCAL → not a speaker → out") it would have excluded
**Qwen**, which clears F-LOCAL at 14L/19L@1e-5 and fails it at mapping@1e-5.
**One run cannot separate "this base can't speak Tlön" from "this config breaks
this base."** Test speakerhood at the forgiving config, never the fragile one.

⭐ **Cross-base is VERDICT-vs-VERDICT, ruler-free** — verified: the lag z comes
from a per-run `permutation_null` ([act2_model_lag.py:186](tools/act2_model_lag.py#L186)),
so no ruler transfers. Magnitudes still do not compare across bases.

⛔ **Do not assume the Qwen fixes transfer.** `eager` was a Qwen+SDPA finding;
each base's first run doubles as its own attention check. The mapping scope must
be verified against **each base's real tensors**.

### ⛔ Scale-axis constraints, measured not assumed

```
Qwen2.5-1.5B / 3B   tie_word_embeddings=True   -> mapping_scope REFUSES
Qwen2.5-3B          36 layers > 7B's 28        -> "smaller" is DEEPER
14B mapping (1.557 B)   66.8 GiB  FITS
14B top-12 of 48        84.5 GiB  OVER 80
32B mapping            113.2 GiB  OVER 80
```

**Down-scale is disqualified** (mapping refused, and not a clean scale axis).
At 14B **only the mapping rung fits one card**.

---

## ⭐⭐ Machinery added today (1720 passing)

1. **The mapping scope** — `embed_tokens`+`lm_head`, layers frozen, built
   AROUND the zero-trainable guard. Positive per-leaf selection; a **tied**
   checkpoint is REFUSED (not silently halved); a leaked layer tensor is
   refused. Verified against the model's **real 339 tensors**.
2. **`mapping_moved` now compares each leaf to its OWN prediction** —
   `lm_head` dense (~1.0), `embed_tokens` sparse (= corpus coverage, measured
   per run by `act2_vocab_coverage.py`). The old `changed > 0` passed rung 2's
   0.49% and **could not say whether that was healthy sparsity or a dead
   tensor** until the corpus was measured afterwards. A guard whose PASS needs
   a follow-up investigation is not yet a guard.
3. **`lint_heredoc_backslash.py`** — no backslash in any heredoc payload,
   absolutely. ⚠️ **It covers `.sh` files and CANNOT see a heredoc typed into a
   tool call, which is where all seven occurrences happened.**
4. **`tests/textguard.py`** — `code_only` / `one_call`. A whole-file text guard
   matches its own documentation; that happened **five times in one session**.
5. **`weight_arm_entry` takes `scope_mode`** — the rule re-derived, not
   stretched, after the scope widened to a second locus.

---

## ⏭ OPEN

1. ✅ ~~Fix `permutation_null`~~ — **DONE**, `docs/UNSCOREABLE_LAG_2026_09_09.md`.
2. **Run 0 has no verdict.** Decide whether a re-read of the persisted
   `fwmap6-s20624` weights is worth a box, or whether F-LOCAL failing at both
   LRs is already the answer. ⭐ **A re-read is now cheap and safe** — the
   instrument survives a degenerate speaker and writes a report either way.
3. **Run 2's family** — licence check, then Nate's pick.
4. **Operationalise "materially"** before any fork comparing two lag-2 values.
5. **The ledger is not persisted** — `f_local` is a verdict input whose row dies
   with the box.
6. **§7.1 all-28 via FSDP** — untested, negative-EV on capacity grounds.
7. **Three decisions from `RESUME_2026_09_04.md`** remain untouched.

---

## Cost

**~$26 on 09-08/09** — rung 1b′ attempt 1 ($9, lost to the persist wall),
attempt 2 ($9), rung 2 ($8, rescued rather than re-fired). Run 0 ~$6 in flight.
~$46 across the arc.
