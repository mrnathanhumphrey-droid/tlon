# REVIEW — the 13.2 arms. **BOTH REVIEWED.**

**Date:** 2026-08-23 · Reviewer: **Nate** (source-holder) · distance-check veto
run by **Wilson** · files: `referents_residue_lyric.yaml`,
`referents_residue_random.yaml`, both now `review_status: REVIEWED`.

⛔ **Regenerating either file resets it to UNREVIEWED, by design.** The
generator never writes REVIEWED, so a rebuilt roster cannot inherit an old
blessing.

---

## Distance check — PASSED

Every coordinate and distance below was **re-derived from the built YAML**, not
copied from the review message. All seven cited coordinates and all four cited
L1 distances matched exactly.

| check | verified |
|---|---|
| attractor at origin — C6M1 *liberation* | `(0,0,0)` ✅ |
| H1 anchor at L1 = 1 — C1M1 *tender-holding* | `(0,1,0)`, L1 from origin = **1** ✅ |
| gross ineffable→material spine reads along **z** | ✅ |
| within-cluster gradients graded and directionally true | ✅ |
| C4 ↔ C2 far (outward/material vs interior/ineffable) | ✅ |

⭐ **Two precisions, recorded because the review message rounded them.**
C4 × C2 pairwise L1 spans **5–8**; 8 is the *maximum* pair, not the cluster
separation. And the furthest two points in the whole set are **C6M1 (the
attractor) ↔ C7M1 (raw grief) at L1 = 9** — the most-liberated and the
most-lost, maximally apart. Neither changes the verdict; both are the kind of
figure that decays into a wrong quote if only the rounded version is on record.

---

## Flag 1 — C3M2 ≈ C2M3 at L1 = 1: **a FEATURE, kept**

`C3M2` *shared communion* `(0,3,0)` and `C2M3` *seeing as gift to the other*
`(0,3,1)`.

Nate's ruling: communion-through-craft and tenderness-given-away **genuinely
evoke nearly the same thing.** This is real cross-cluster evocative proximity —
two referents in different denotation-clusters that gesture at almost the same
place.

⭐ **That is precisely what a metric residue can express and a categorical one
cannot**, so it is evidence the arms differ in the intended way, not a
collision. The loader's duplicate check is *within*-cluster only, correctly:
coordinates may repeat across clusters and near-repeats are meaningful.

---

## Flag 2 — C8M2 *love-as-risk* `(2,2,4)`: **HUMAN OVERRODE THE AI DISTILLER**

⭐⭐ **This is the load-bearing provenance entry. Read it before quoting the
distillation's credibility anywhere.**

**Wilson's flag.** `C8M2` sits nearer raw-grief `C7M1` (L1 = **3**) than its own
cluster-mate love-as-strength `C8M1` (L1 = **5**). On a "it's all love, so the
love cluster should hang together" reading, that looks misplaced.

**Nate overrode it, with reasoning:**

> **risk → fear → grief.** Love-as-risk holds **live fear**; love-as-strength is
> **fear resolved**; raw grief is **fear realised**. On fear-content, risk and
> grief are neighbours and *strength* is the outlier.

**The placement stands, unchanged.** The "it should cluster with its mate" read
was shallower than the source-holder's, and the axis that actually organises the
neighbourhood is fear-content, not topic.

**Why this entry matters more than the coordinate it defends.** Per **D11**, the
24 coordinates are **Code's projection of Nate's briefed geometry** — one human
geometry plus one mechanical embedding, *not* two independent distillations. So
the standing question about this arm is whether a human was ever actually in the
loop or merely rubber-stamped a machine's output.

> ⭐ **Here the human moved against the machine, on a stated substantive ground,
> and the machine's placement survived on the human's reasoning rather than its
> own.** The veto fired. That is the difference between review and assent, and
> it is the evidence that the distillation has a human in it.

⛔ **It does NOT make this two distillations.** The Mantel test still requires a
second *human* matrix over the same 24 descriptions. Nobody writes "two
distillers agreed."

---

## Housekeeping applied

**The lyric header said 2-D and the coordinates are 3-D.** Fixed in the
generator (so it survives regeneration) and rebuilt; coordinates verified
identical across the rewrite. The header now carries the 5×5×5 lattice, the
no-clipping arithmetic (`span × dim = 12`), the reason 2-D was abandoned (96 %
occupancy ⇒ graded judgment inexpressible), **and the axis frame as built** — so
a second Mantel distiller reads accurate placement instructions rather than the
brief that was superseded.

⭐ The header also now states that the 5→3 projection is **Code's**, and invites
a second distiller to reject the frame rather than force a placement into it.
