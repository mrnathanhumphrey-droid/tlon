# RESUME — 2026-09-06

⭐ **START HERE.** Supersedes `RESUME_2026_09_04.md` for anything about the
full-weight arm; that file is still correct for the dose-slope work and the three
open decisions listed in it.

---

## ⏭ THE ONE OPEN THREAD — a named LOCATION, and a mechanism still open

The full-weight arm's training loop produces a NaN model. **Seven mechanisms are
excluded by positive test.** As of the `fwpreclip-s20624` run the fault is
located exactly:

> **The NaN is produced inside layer 27's self-attention BACKWARD, on the
> gradient w.r.t. Q. `grad_K` and `grad_V` off the same attention are FINITE.
> Everything downstream of that point — layers 26 down to 14 — is NaN.**

⛔⛔ **THE PREVIOUS HEADLINE WAS WRONG AND IS RETRACTED.** It read: *"a single
tensor's gradient overflows and `clip_grad_norm_`'s global norm spreads it to all
168."* The pre-clip probe built to confirm that disconfirmed it in its first row:
at step 13, **159 of 168** gradients were already non-finite **before** the clip.
The clip added **9**. It is a spreader, but it was never doing the work that
story gave it.

⭐⭐ **The arrival order inside the backward is a clean cut at position 9 of
168** — every tensor before it finite, every tensor after it NaN, zero
exceptions. See §7 of `FINDINGS_FULLFT_TRACE_2026_09_06.md` for the table.

⭐ **TRIGGERED, NOT COMPOUNDING.** `L27.q_proj.weight`'s pre-clip gradient absmax
ran 0.0109 → 0.0104 → 0.0137 → 0.0134 → 0.0138 → 0.0085 → 0.0074 → **NaN**, and
no tensor in the model ever exceeded 0.19. It was *declining*. There is no
runway, so the whole compounding fix class — per-layer LR, per-tensor clipping
ahead of the global norm, LR reduction — **is closed**.

### ⛔ And the leading hypothesis for the mechanism is already dead, for $0

The asymmetry (grad_Q NaN, grad_K/grad_V finite, fused SDPA backward, triggered
on one deterministic step) is the known shape of a **fully-masked query row**.
Tested locally with no GPU and no model weights, against a byte-identical corpus
rebuild (sha `dd40e22f85b0b6e4`) and the real tokenizer and collator:

- `padding_side` is **right**, so key 0 is always a real token
- **minimum attendable keys over every (batch, query) pair = 1, never 0** — in
  the worst-case batch (longest row with the three shortest) and across a sweep
  of the corpus
- 0 rows with all labels masked; lengths 55–317 against a 384 cap, nothing
  truncated, nothing degenerate

**No query row in this corpus can be fully masked.** ⭐ The $0 test killed the
$1 run's hypothesis before it was bought.

### ⏭ What is actually next

The location is measured; the **mechanism is not**. Under the textbook attention
backward, `dQ = dS @ K` — so `grad_K` finite implies `dS` finite, and `dS` and
`K` both finite should leave `dQ` finite. **The observation and the textbook
disagree**, which means the answer is in what the fused kernel actually does, not
in what attention math says it should.

Two cheap, decisive moves, ~$1 each on a box (the whole run is 460 s and the
break is at step 13, so 15 steps suffices):

1. **Tensor-level backward hooks inside layer 27's attention** — on `q_proj`'s
   output, the RoPE output, and SDPA's outputs — to see whether `dQ` arrives NaN
   *from* SDPA or becomes NaN *between* SDPA and `q_proj`.
2. **`attn_implementation="eager"` as a one-bit ablation.** Eager breaks at 13
   too → the fused kernel is exonerated and the fault is in the model math.
   Eager trains clean → the kernel is implicated. Every run so far used the
   default SDPA, so it has been a **constant, never a variable**.

⛔⛔ **NONE OF THIS IS ABOUT TLÖN.** D6's architectural floor is untouched. This
is a bug in machinery written on 2026-09-05/06, and the full-weight arm has
never produced a readable result because the loop has not yet run clean.

---

## ⛔⛔ THE CORRECTION THAT MATTERS MOST

**The founding symptom never happened.** `logging_nan_inf_filter` defaults to
`True`: when the loss is NaN or Inf, transformers **substitutes the running
average and logs that**. So the first run's "loss 7.548 → 0" was the filter, not
a collapse — the loss went NaN and the substituted average decayed toward zero.

Hours of hypotheses (degenerate objective, label path) were explaining a logging
artifact. **And the first version of the step trace inherited the same flaw** —
it read the loss from `on_log`, so its row 13 showed `loss=0.6404, finite=True`
while every gradient in the model was already gone. *The trace was showing the
mask.*

⭐ Fixed structurally: the raw loss is captured from the model output by
wrapping `compute_loss`, `logging_nan_inf_filter` is off while tracing, and the
framework's value is retained under the key **`loss_LOGGED_FILTERED`**. The
field name `loss` no longer exists in a trace row.

⭐⭐ **The rule to carry: a framework's logged or summarised value is a proxy;
the value the model returned is the arbiter, and the two diverge exactly when
something is wrong — which is precisely when anyone is looking.**

---

## What the evidence says now

| step | forward | backward | after optimizer |
|---|---|---|---|
| ≤ 12 | clean; activation absmax 8128 (`L3.mlp.down_proj`), stable | finite; all 168 gradients small and DECLINING (max 0.19) | clean |
| **13** | **clean — RAW loss finite (0.1109)** | **NaN enters at `grad_Q` in L27 attention → 159/168 non-finite PRE-clip; the clip adds the last 9** | 70 weights poisoned |
| 14 | NaN (a *consequence* of the NaN weights) | — | — |

⛔ **The 1-D/2-D fingerprint was never a localiser.** The "70 weights and not
168" split describes which optimizer-state path tolerates a NaN gradient —
tensors below `min_8bit_size` (4096) take the poisoned update on the 32-bit path,
the 98 matrices ride bitsandbytes' 8-bit blockwise path and absorb it. Four
hypotheses were built on that split before the trace showed all 168 gradients
were already gone.

⛔ **And "all 168 at once" was not one event globalised, either** — 159 of them
were independently NaN inside the backward. Both readings mistook a downstream
artifact for a cause; the arrival-order cut is the first evidence that points at
an origin rather than at an aftermath.

---

## ⛔ SEVEN MECHANISMS EXCLUDED BY POSITIVE TEST

1. **8-bit quantisation of the 1-D params** — they never used 8-bit state;
   `min_8bit_size` is 4096 and every 1-D tensor here is 3584 or 512.
2. **Size-dependent NaN propagation** — one NaN gradient destroys 512, 3584 and
   8192 alike.
3. **Second-moment underflow** — 300 steps at gradient 1e-12, 1e-20 and exactly
   0 all stay finite.
4. **The label path** — 1297/2216 positions unmasked; `pad_token` (151643) ≠
   `eos` (151645). Corpus scan: **0 zero-token, 0 truncated**, lengths 55–317
   against a 384 cap — re-verified locally 2026-09-06 against a byte-identical
   rebuild (sha `dd40e22f85b0b6e4`).
   ⚠️ **The row count in this line was wrong: it read 20,002.** The corpus is
   **60,148 train / 1,227 eval**, counted off the verified rebuild. The scan's
   FINDINGS reproduce exactly; only its denominator was wrong, and the
   provenance of 20,002 could not be recovered. See [[resolution_match]].
5. **The assembly at toy scale** — 16-cell ablation over mixed-dtype ×
   checkpointing × input-require-grads × autocast: no NaN anywhere, loss
   descends identically in all 12 runnable cells.
6. **Checkpointing × frozen stack, at real scale** — with gradient checkpointing
   OFF *and* `enable_input_require_grads()` not called, the failure is
   byte-identical: gradient non-finite at **step 13**, **168 tensors**, same
   first-tensor list.

7. **The fully-masked attention row** — `padding_side` is right, and the
   minimum attendable keys over every (batch, query) pair is **1, never 0**,
   measured locally against a byte-identical corpus rebuild. Costed $0.

⭐ **The determinism is the asset.** Two runs, different assemblies, identical to
the step. This is not a flaky failure being sampled — it is a fixed event, so the
instrument can be pointed rather than swept.

⛔ **"bf16 overflow" is now EXCLUDED, not merely discouraged.** bf16 has the
*same exponent range as fp32*, and the post-clip signature settles it: an `inf`
origin would have left every other tensor at exactly zero and finite, which is
not what the run reported. See §6 of `FINDINGS_FULLFT_TRACE_2026_09_06.md`.

⛔ **A third instrument bug, fixed while building the probe.**
`ForwardProbe.snapshot()` answered outside its own window, so **rows 15–19 of the
raw-loss trace are five identical copies of step 14** wearing later step numbers.
They are not measurements. ⭐ Three for three: *an instrument that keeps
answering after it stops looking reports its own memory as data.*

---

## Runs and cost

| box | what | outcome |
|---|---|---|
| `cd5a786b` | first full-weight run, 1 epoch, `2d80803` | NaN model; §4.1 wrongly returned OK |
| `d671bfe4` | trace attempt 1 | died on a duplicate `logging_steps`; **stranded** (see below), terminated by hand |
| `0978c1cb` | trace, real config, 300 steps | `FIRST_NONFINITE: step 13, grad, 168` |
| `cd24d84e` | ablation, checkpointing OFF | identical failure — checkpointing cleared |
| `9cefd027` | raw loss + activation absmax | raw loss NaN at **14**, gradients at **13** → backward-only |

**~$14 total.** All self-terminated after persisting except `d671bfe4`.

⛔ **The stranding, banked:** its watchdog was killed to reuse the box, and then
SSH became unreachable — leaving a billing H100 with no safety net and no
access. **Verify continued access BEFORE removing the net, or simply let the
watchdog terminate the box and relaunch.** Saving a 15 GB download was not worth
it.

---

## Artifacts (hub-verified, `hf://keyzersoze04/tlon-act2-adapters`)

- `fw-s20624/` — the first NaN model (21.8 GB) + `weight_delta.json`
- `fwtrace-s20624/step_trace.jsonl` — 300 steps, the ordering result
- `fwnockpt-s20624/` — the checkpointing ablation
- `fwraw-s20624/` — raw loss + activation magnitudes
- ⛔ `fw-s20624`'s **pipeline log is lost** — the flush named one filename. Fixed
  in `bbfff69`; every later run's log reached the hub.

---

## Records

- `docs/PREREG_FULL_FINETUNE_RELEASE_2026_09_05.md` — **LOCK `a0450b36`**,
  verifies. Not fired: no run has produced a readable verdict.
- `docs/DEVIATIONS_FULL_FINETUNE_2026_09_05.md` — D-1..D-6.
- `docs/FINDINGS_FULLFT_TRACE_2026_09_06.md` — the ordering result and the
  fingerprint correction.

## Still true, still not mine

**`rsna-3darms`** has been active in us-east-1 at $1.99/hr throughout. Different
project; untouched. `MEMORY.md` says RSNA had nothing in flight as of 09-04, so
that entry is either stale or something started since — worth a look.
