# RESUME — 2026-09-04

⭐ **START HERE.** Then `STATE.md` (entry), `MEASUREMENTS.md` (dictionary),
`DECISIONS_2026_09_04.md` (this arc) and `DECISIONS_2026_09_03.md` (the one
before), `runs/act2/retrain12/SALVAGE_2026_09_04.md` (what was lost).

---

## ✅ THE DOSE SLOPE READ: **ARCHITECTURAL FLOOR, DECISIVE**

Prereg `docs/PREREG_RELEASE_SUPPRESSION_DOSE_SLOPE_2026_09_04.md` **LOCK
`765b6787`**. Full read in `DECISIONS_2026_09_04.md` **D6**.

| dose | corpus lag-2 | corpus z2 | model lag-2 | model z2 | model z1 |
|---|---|---|---|---|---|
| −1 `cp-s20624` | 0.443 | **+200.86** | 0.337 | +5.40 | +23.60 |
| 0 `ct-s20624` | 0.027 | −11.22 | 0.385 | +6.56 | +22.86 |
| +1 `ctw1-s20624` | 0.000 | **−29.13** | 0.333 | +5.30 | +22.58 |

⭐⭐ **THE TRAINING DATA SWUNG 0.443 → 0.000 SHARED ROOTS AT LAG 2. THE MODEL
LANDED ON ≈0.35 EVERY TIME**, unordered. corpus z span **229.99**; model z span
**1.26**, and dose 0 is the HIGHEST with both extremes below it.
Δ_model(−1 vs +1) = **+0.09**.

⛔⛔ **THE LAG-2 PERSISTENCE IS ENDOGENOUS TO THE BASE WEIGHTS.** It is not
coming from the corpus at any achievable dose. **The flatness is the evidence.**

⛔ The twelve-adapter batch is NOT bought — not at any dose. The chatbot is NOT
unblocked on this path. Corpus-signal strength is **exhausted as a lever**, and
that is now measured rather than suspected.
⭐ Rank was held fixed on purpose, so this says NOTHING about capacity. A
LoRA-rank sweep or a heavier intervention is the next question and a SEPARATE
decision.
⚠️ Not claimed: that the gain is exactly zero. At 12 chains the 1.26 spread is
consistent with pure noise. Claimed: any channel is smaller than this read's
noise across the entire achievable corpus range.

---

## ✅ EARLIER — the dose boxes (both self-terminated after persisting)

**The release-suppression DOSE SLOPE.** Prereg
`docs/PREREG_RELEASE_SUPPRESSION_DOSE_SLOPE_2026_09_04.md` **LOCK `765b6787`**
(verifies). One knob: `suppression_window`.

| dose | instance | ip | corpus sha | recipe |
|---|---|---|---|---|
| **−1** low anchor | `5484bbbfd62c427ca6223d71645953e5` | 129.146.67.245 | `61d14bdf` | content-persistent |
| **0** the gate | *already measured* | — | `dd40e22f` | content-transient |
| **+1** ceiling | `a511bc89edc346c2a7ce441ea7b1a0b7` | 158.101.18.26 | `897034bb` | content-transient |

⭐ **WHY A SLOPE AND NOT A LADDER.** The corpus-side curve
(`runs/act2/dose_curve_corpus.json`) was measured FREE, before any GPU, and
found: (1) **upward the axis has exactly one step** — window 1 already drives
self-overlap to 0.0000 and windows 2+ are numerically identical, so there is no
ladder; (2) **perceive is invariant across the whole range** (echo_rate 1.000 →
0.949, lag-1 z +385 to +428), which **eliminates the ENTANGLED fork corpus-side**.
Hence the span was extended DOWNWARD: corpus lag-2 z **+162.43 → −19.73**, a
**182-z span**, and the model-side read is a slope.

⛔⛔ **THE READ** — at dose 0 the model's lag-2 was **0.385** against a corpus
**0.027**, i.e. **≈14× its own training data**: it GENERATES persistence the
corpus does not contain. So:
* model lag-2 **tracks** the corpus → transmission works, gain < 1
* model lag-2 **flat** across 182 z → **ARCHITECTURAL FLOOR, decisive** — the
  persistence is endogenous to the base weights and **the flatness is the
  evidence**

⛔ `cp-s20624` is a **DOSE ARM, NEVER A CELL**. `content-persistent` is absent
from `RECIPES`, every factorial entry point validates against that tuple, and
`dose_arm_entry` omits `cell` / `factorial_pair_key` /
`pairing_capability_side`. Structurally un-poolable, not merely labelled.

---

## ✅ EARLIER TODAY — both of those boxes finished and self-terminated

Launched 2026-09-04 ~13:2x UTC, both `gpu_1x_a100_sxm4` / `us-west-2` / $1.99 h.

| run | instance | ip | expect |
|---|---|---|---|
| ~~`solo_regen`~~ ✅ | `30abb29e…` — **DONE 17:30 UTC, self-terminated** | — | ran 4.2 h |
| ~~`retrain12_ct`~~ ✅ | `f4873ab7…` — **DONE 18:12 UTC, self-terminated** | — | ran 4.8 h |

    python tools/act2_retrain_orchestrate.py poll --host <ip> --root <run>
    python tools/act2_provision.py            # (status via `... status`)

⭐⭐ **IF THEY ARE GONE, NOTHING IS LOST — THAT IS THE POINT OF THIS ARC.** The
boxes persist their own artifacts to `hf://keyzersoze04/tlon-act2-adapters` as a
**pipeline stage**, per adapter, and `~/DONE` is written only after a verify
stage confirms it. Restore with:

    python tools/act2_box_persist.py --root . --repo keyzersoze04/tlon-act2-adapters \
        restore --into <dir> --pattern '<cell-or-run>/*'

⛔ Terminate deliberately when they are done — the watchdogs do it themselves on
DONE, on a stall (60/90 min), and on a deadline (12/40 h), but check `status`.

---

## WHERE THE THREE-ITEM QUEUE STANDS

**1 · Fix the termination architecture — ✅ DONE.** `bba4ef9`, `07c5489`,
`5de2e59`, `1ee9827`, `4248023`, `5f22600`. Suite 1487 → 1526.
See `DECISIONS_2026_09_04.md` D1/D2 and R1-R5 for what the sweep turned up.

**2 · Regenerate the 84 solo transcripts — ✅ DONE 17:30 UTC.**
`pipeline_solo_regen.sh` restored the six survivors FROM THE HUB (first real use
of the recovery path), md5-pinned them against `SALVAGE_2026_09_04.md` §1 —
*"6 adapters md5-pinned against the salvage record and mutually distinct"* —
then ran the asym_recert SOLO procedure, ~37 min per build, persisting per build.

⭐ **VERIFIED FROM THE HUB, not from the box's own report:** 84 transcripts,
14 per build, **40 surfaces each**, counted through `_transcript` — the ruler's
own accessor — and structurally identical to the reference population
(`asym_recert/logs/s20621_solo_1.json`). Extracted to
`runs/act2/solo_regen/logs/` (gitignored; re-fetch from
`hf://…/solo_regen/<build>_solo_logs.tar.gz`).

⭐⭐ **AND THE WHOLE ARCHITECTURE CHANGE WORKED ON ITS FIRST REAL RUN.** The box
persisted per build, gated `~/DONE` behind `verify_persisted`, its watchdog saw
DONE, ran the **flush** (which is why `watchdog.log` is on the hub), and
terminated itself. That exact sequence is what cost 84 transcripts the night
before. Nothing had to be collected by hand.

⛔ NOTE FOR THE RULER: `ASYM_BUILDS` is a PINNED list of seven builds in
`asym_recert/logs`, and that pin is correct — it is what "frozen ruler" means.
Folding these six in takes it 7 → 13 and MOVES it. `load_builds(spec=...)`
already accepts a spec; `act2_distance.main()` just never exposes one. ⭐ That is
a deliberate RE-BASELINING with a `FLOOR_ka` recompute attached, not a code fix.

**3 · Re-run the gate — ✅ DONE 18:12 UTC. ⛔⛔ THE VERDICT IS `REFUSED`.**

See `DECISIONS_2026_09_04.md` **D5** for the full read. In one line:
**responsiveness transmitted, release did not.**

| lag | CORPUS | MODEL |
|---|---|---|
| 1 | 0.962 z **+516.30** | 1.028 z **+22.86** ✅ |
| 2 | 0.027 z **−11.22** | 0.385 z **+6.56** ⛔ ceiling 3.0 |
| 3 | 0.042 z −3.87 | 0.238 z +2.43 |
| 4 | 0.048 z −0.70 | 0.222 z +2.08 |

12/12 chains, **0 dropped**, 120 turns. F-LOCAL render 1.000 / speak 1.000.
Corpus rebuilt deterministically and sha-verified `dd40e22f85b0b6e4`.

⭐ **The first-order question is ANSWERED YES** — corpus lag-1 responsiveness
does reach the model (content-free gave 0.00 shared roots; this gives +22.86).
⛔⛔ **But lag 2 is the speaker's OWN previous turn**, and the model holds it:
object permanence, the thing Tlön denies. The recipe did not fail toward the
control, it **overshot past the target**.

⛔ **The eleven are NOT bought. The chatbot is NOT unblocked. NO THRESHOLD
CHANGE IS AUTHORISED** — and 6.56 against 3.0 is not a near miss anyway.

### WHEN THE GATE LANDS — READ IT AGAINST THE LOCK, NOT AGAINST THE TRANSCRIPT

`docs/PREREG_CONTENT_TRANSIENT_MODEL_GATE_2026_09_03.md` **LOCK `abde6124`**
(verifies; 16/16 locks verify). Output: `model_lag_ct-s20624.json`, on the hub
under `ct-s20624/`.

| lag 1 | lag ≥2 | verdict |
|---|---|---|
| z ≥ 6.0 | all z ≤ 3.0 | **GO** — train the eleven; the chatbot is unblocked |
| z < 6.0 | all z ≤ 3.0 | STOP, washed out — the fine-tune is the obstacle, not the corpus |
| z ≥ 6.0 | any z > 3.0 | STOP, persists — object permanence, **worse than the control** |
| z < 6.0 | any z > 3.0 | STOP, both — instrument or training fault first |

⛔⛔ **A STOP DOES NOT AUTHORISE A THRESHOLD CHANGE.** The thresholds are hashed
into the locked body. A lag-1 z between 3 and 6 is **UNDERPOWERED, not a STOP by
magnitude** — the honest follow-up is more chains, pre-declared.
⛔ **A GO is not "the transcripts read well."** The verdict is the lag profile.

---

## WHAT IS DURABLE

**Six control adapters** `s20624`–`s20629`, each 323,014,168 bytes, md5-verified
against box-computed checksums, on `hf://keyzersoze04/tlon-act2-adapters` and
hub-verified. ⭐ **Independently re-verified today** — the solo_regen box pulled
all six from the hub and re-checked them against the salvage record: *"6 adapters
md5-pinned against the salvage record and mutually distinct"*.
⛔ Their `adapter_config.json` is RECONSTRUCTED — see each `PROVENANCE.md`.
⛔ They have no `factorial.json` (it postdates their run); they are the
**content-free / legacy single-stream** arm, paired by seed only.

**F-LOCAL, all six clear** (n=64, cardless, unconstrained): .953 / .953 / .938 /
.984 / .953 / 1.000 render; speak 1.000 throughout. ⚠️ Transcribed from a box log
that was never pulled — `SALVAGE_2026_09_04.md` is the only record and says so.

## THE ARC'S STANDING RESULTS

- **content-transient corpus works.** ⛔⛔ **THREE figures now wear one name** —
  prereg §1 quotes **+120.36**; the LOST gate corpus (train sha `7611772c…`)
  recorded **518.64**; the corpus actually trained on (train sha `dd40e22f…`,
  rebuilt and verified) measures **+516.30**. All at lag 1, all "the corpus lag-1
  z". ⭐ They are different BUILDS, so the number belongs to the build: quote it
  as `z(dd40e22f) = +516.30`, never as "the corpus z".
- **content-free is the control**, measured off its own rows: s20621 −0.60 ·
  s20622 −0.17 · s20623 −0.03 · t30001/2/3 −0.13.
- ⛔ **The factorial pair key is the CORPUS seed, not the adapter name.**
- ⛔ Pairing is a property of the PAIR; a MISSING generator field means LEGACY.
- **The content-transient recipe costs nothing in fluency — now on a record, not
  in a memory.** The 2026-09-04 gate run's `ct-s20624` cleared F-LOCAL at
  **render 1.000 / speak 1.000** (64/64, battery `d9ecdf9fbf2caa8a`, cardless and
  unconstrained; Amendment A gate 59.4%, clear).
  ⛔ This neither confirms nor refutes the lost run's transcribed 0.969: that was
  a DIFFERENT adapter (different corpus draw, and GPU training is not
  bit-deterministic). It corroborates the CLAIM, not the number — and the number
  itself remains an uncorroborated transcription, see `SALVAGE` §3's correction.
- ✅ **ANSWERED 2026-09-04: yes, lag-1 responsiveness reaches the model**
  (z +22.86). ⛔⛔ **And so does persistence the corpus suppressed** —
  model lag 2 z +6.56 against a corpus lag 2 of z −11.22. The open
  question is now: **why does the fine-tune not preserve the release?**

## THE LESSON, CARRIED FORWARD AND EARNED AGAIN TODAY

⭐ **When a defect is found, sweep for its class before moving on.** Yesterday's
two losses were classes already met and fixed in one place. Today the sweep found
four more instances — and the guard written for one of them found the next.

⭐ **And one level past it:** simulate the target environment, then **check the
simulation is faithful**. Today's sim disagreed with the box (24 failures vs 2);
the sim was the liar, and chasing why exposed that the language's identity hash
depended on which OS cloned the repo.
